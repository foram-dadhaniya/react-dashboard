export interface UserInputValues {
    id: string,
    name: string,
    email: string,
    birthdate: string,
    number: string,
    
}

export interface ProjectInputValues {
    id: string,
    projectName: string,
    startDate: string,
    endDate: string
}